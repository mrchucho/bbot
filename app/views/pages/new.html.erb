<% title("new page") %>
<h1>New page</h1>

<%= error_messages_for :page %>
<% form_for(@page) do |f| %>
  <%= render :partial => 'page_form',:object => @page,:locals => {:f => f} %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>
